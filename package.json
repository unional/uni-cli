{
  "name": "root",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "build": "tsc -b",
    "build:watch": "tsc -b -w",
    "lint": "eslint packages/**/*.ts",
    "test": "jest",
    "test:watch": "jest --watch",
    "verify": "yarn test",
    "watch": "yarn test:watch"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^2.11.0",
    "@typescript-eslint/parser": "^2.0.0",
    "eslint": "^6.1.0",
    "eslint-plugin-harmony": "^2.0.1",
    "jest-junit": "^7.0.0",
    "jest-watch-toggle-config": "^1.0.2"
  }
}
