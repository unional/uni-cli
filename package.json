{
  "name": "root",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "build": "tsc -b",
    "build:watch": "tsc -b -w",
    "test": "yarn jest",
    "verify": "yarn test",
    "watch": "yarn jest --watch"
  },
  "devDependencies": {
    "jest-junit": "^7.0.0",
    "jest-watch-toggle-config": "^1.0.2"
  }
}
